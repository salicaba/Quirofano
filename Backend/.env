#=========================Logica de Conexion con Postgress==================================#
#Puerto de la aplicacion
PORT=4001

# Usamos la IP de Windows y el puerto de PostgreSQL
DB_HOST=172.29.112.1 # <-- La IP de windows o donde este la base de datos
DB_USER=postgres      # <-- O tu usuario específico (En postgres por lo regular es el mismo)
DB_PASSWORD=28112005  # <----- La contraseña de Postgress
DB_NAME= quirurjicofinal(ojala) #<------------ Nombre DB
DB_PORT=5432          # <-- Puerto de PostgreSQL

#Entorno
NODE_ENV=development


FRONTEND_URL=http://localhost:5174

JWT_SECRET=se_supone_que_esto_tiene_que_ser_muy_largo
